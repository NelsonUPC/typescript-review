{
  "name": "typescript-review",
  "version": "1.0.0",
  "description": "Typescript Sample Project, illustrating fundamental aspects of the language, like classes, attributes and methods.",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "typescript": "^5.4.4",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/uuid": "^9.0.8"
  }
}
